{
  "name": "bootstrap-sandbox",
  "version": "1.0.0",
  "scripts": {
    "start": "rm -rf .parcel-cache && parcel serve src/index.html --public-url / --dist-dir dist",
    "build": "rm -rf .parcel-cache && parcel build src/index.html --public-url / --dist-dir dist",
    "open:chrome": "npx opn-cli http://localhost:1234 --browser=chrome",
    "serve": "npm-run-all --parallel start open:chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^6.0.0",
    "@parcel/transformer-sass": "^2.12.0",
    "npm-run-all": "^4.1.5",
    "opn-cli": "^5.0.0",
    "parcel": "^2.12.0"
  }
}